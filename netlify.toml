# Static HTML site (Redox template) — no build step, publish the repo root.
[build]
  publish = "."

# Long-cache the template assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"
