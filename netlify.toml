# Static HTML site (Redox template) — no build step.
[build]
  command = "echo 'Static site — no build needed'"
  publish = "."

# Long-cache immutable-ish assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"
